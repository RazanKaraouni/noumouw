<?xml version="1.0" encoding="utf-8"?>
<!-- Debug/profile only: allow HTTP to local backend (emulator, LAN IP, localhost). -->
<network-security-config>
    <base-config cleartextTrafficPermitted="true" />
</network-security-config>
