<?xml version="1.0" encoding="utf-8"?>
<!-- Profile builds on a physical phone need HTTP to the PC LAN IP, not only localhost. -->
<network-security-config>
    <base-config cleartextTrafficPermitted="true" />
</network-security-config>
